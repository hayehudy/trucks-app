import { StyleSheet } from "react-native";

const StartStyles = StyleSheet.create({
  container: {
    backgroundColor: "#fff",
    alignItems: "center",
    height: "100%",
  },

  Btnstart: {
    marginTop: 80,
  },

  loginText: {
    marginTop: 50,

    textAlign: "center",
    fontSize: 50,
    color: "red",
    fontWeight: "bold",
  },
});

export default StartStyles;
